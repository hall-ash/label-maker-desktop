{
  "name": "make-labels",
  "version": "1.0.0",
  "description": "Label Making App",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "start": "electron .",
    "dev": "concurrently \"cd frontend && npm start\" \"wait-on http://localhost:3000 && npx electron .\""
  },
  "dependencies": {
    "electron-store": "^10.1.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "cross-env": "^10.0.0",
    "electron": "^37.2.6",
    "electron-builder": "^26.0.12",
    "wait-on": "^8.0.3"
  }
}
